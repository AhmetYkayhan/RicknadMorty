# AuthFeature

Authentication feature module using VIP-S (Clean Swift for SwiftUI) architecture. Contains Login scene with Interactor, Presenter, Models, and View. Depends on Firebase Auth for authentication.
