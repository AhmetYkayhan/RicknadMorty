# DesignSystem

Reusable UI components and design tokens for the RicknadMorty app.

Includes color palette (`AppColors`), spacing scale (`AppSpacing`), typography tokens (`AppTypography`), and shared components (`AppButton`, `AppTextField`, `ErrorView`, `LoadingView`).

## Dependencies

None — this package is fully standalone (SwiftUI only).
